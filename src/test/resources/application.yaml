# src/test/resources/application-test.yaml
# 테스트 환경 기본 설정 — 환경변수 없이 ./gradlew test를 실행해도 성공하도록 한다.
# CI의 EMAIL_VERIFICATION_ENABLED=false와 동일한 효과.
spring:
  mail:
    host: ""

app:
  email-verification:
    enabled: false
    base-url: http://localhost:8080
