# CLAUDE.md — Vium 프로젝트 규칙

담당: 박찬 (백엔드A). 이 저장소에서 작업할 때 아래를 지킨다.

## 절대 금지

- **D-005**: 컨디션 추론(FR-003, `POST /checkins`)에서 외부 LLM을 호출하지 않는다. 규칙 기반 분기만 사용.
- **Flyway 마이그레이션 파일(`V*__*.sql`) 생성/수정 금지.** 스키마 소유자는 백엔드B. 필요하면 무엇이 왜 필요한지만 남기고 파일은 만들지 않는다.
- `dev`, `main` 브랜치에 직접 push 금지. 항상 기능 브랜치 → PR.

## Git / GitHub

- 브랜치 전략: 1 기능 = 1 브랜치 = 1 PR. base는 `dev`.
- 커밋 메시지: `[Prefix] #이슈번호 - 내용`.
- **이슈·PR은 실제로 GitHub에 올리기 전에 반드시 사용자 컨펌을 받는다.** (`gh issue create`, `gh pr create` 등은 작업 마무리 단계에서 초안을 먼저 보여주고 승인받은 뒤에만 실행)

## 사용자 입력이 필요한 작업

개인 API 키·서버 접속정보 등 사용자 본인 입력이 필요해 진행이 막히는 작업은 중단하지 말고:

1. `.claude/docs/05-blocked-on-user.md`에 무엇이 왜 필요한지 기록
2. 그 항목은 미뤄두고 막히지 않은 다른 작업을 먼저 진행
